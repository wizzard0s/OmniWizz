{
  "name": "WizzQA Sandbox",
  "image": "://microsoft.com", 
  "remoteUser": "vscode",
  "updateRemoteUserUID": true,
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylance",
        "Continue.continue"
      ]
    }
  },
  "runArgs": [
    "--net=host",
    "--userns=keep-id"
  ],
  "postCreateCommand": "pip install --upgrade pip && pip install psycopg2-binary pytest playwright selenium requests && playwright install --with-deps"
}

